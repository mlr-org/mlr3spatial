# Write a Raster in Chunks

Writes square raster to disk in chunks. Internal helper function.

## Usage

``` r
write_raster(data)
```

## Arguments

- data:

  `[SpatRaster]`  
  `SpatRaster` object.
